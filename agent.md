# Agent entry point

The canonical continuation instructions are in [`AGENTS.md`](AGENTS.md).

Any AI agent or developer must read `AGENTS.md` and then `handoff/README.md` before changing TenantBoundary. This lowercase file exists only because some tools or operators look for `agent.md`; do not duplicate or diverge the source of truth here.
